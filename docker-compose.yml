# docker-compose.yml — SRV_MAIN (192.168.50.10)
# RP06 — PRA/PCA IRIS Nice | NVTech
version: '3.8'

services:
  openldap:
    image: osixia/openldap:1.5.0
    container_name: openldap
    environment:
      LDAP_ORGANISATION: "IRIS Nice"
      LDAP_DOMAIN: "iris.local"
      LDAP_ADMIN_PASSWORD: "adminpassword"
    ports:
      - "389:389"
      - "636:636"
    volumes:
      - ldap_data:/var/lib/ldap
      - ldap_config:/etc/ldap/slapd.d
    restart: always

  phpldapadmin:
    image: osixia/phpldapadmin
    container_name: phpldapadmin
    environment:
      PHPLDAPADMIN_LDAP_HOSTS: openldap
      PHPLDAPADMIN_HTTPS: "false"
    ports:
      - "8080:80"
    depends_on:
      - openldap
    restart: always

  glpi-db:
    image: mariadb:10.6
    container_name: glpi-db
    environment:
      MYSQL_ROOT_PASSWORD: RootGLPI_IRIS_2026!
      MYSQL_DATABASE: glpi
      MYSQL_USER: glpi
      MYSQL_PASSWORD: GlpiDB_IRIS_2026!
    volumes:
      - glpi_db:/var/lib/mysql
    restart: always

  glpi:
    image: diouxx/glpi:latest
    container_name: glpi
    environment:
      MARIADB_HOST: glpi-db
      MARIADB_DATABASE: glpi
      MARIADB_USER: glpi
      MARIADB_PASSWORD: GlpiDB_IRIS_2026!
    ports:
      - "8090:80"
    volumes:
      - glpi_data:/var/www/html/glpi
    depends_on:
      - glpi-db
    restart: always

  nextcloud-db:
    image: mariadb:10.6
    container_name: nextcloud-db
    environment:
      MYSQL_ROOT_PASSWORD: RootDB_IRIS_2026!
      MYSQL_DATABASE: nextcloud
      MYSQL_USER: nextcloud
      MYSQL_PASSWORD: NextcloudDB_IRIS_2026!
    volumes:
      - nextcloud_db:/var/lib/mysql
    restart: always

  nextcloud:
    image: nextcloud:apache
    container_name: nextcloud
    ports:
      - "80:80"
    environment:
      MYSQL_HOST: nextcloud-db
      MYSQL_DATABASE: nextcloud
      MYSQL_USER: nextcloud
      MYSQL_PASSWORD: NextcloudDB_IRIS_2026!
    volumes:
      - nextcloud_data:/var/www/html
    depends_on:
      - nextcloud-db
    restart: always

  wg-easy:
    image: weejewel/wg-easy
    container_name: wg-easy
    environment:
      WG_HOST: 192.168.50.50
      PASSWORD: WireGuard_IRIS_2026!
    ports:
      - "51820:51820/udp"
      - "51821:51821/tcp"
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    restart: always

  clamav:
    image: clamav/clamav:1.4_base
    container_name: clamav
    ports:
      - "3310:3310"
    restart: always

  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    ports:
      - "9443:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    restart: always

volumes:
  ldap_data:
  ldap_config:
  glpi_db:
  glpi_data:
  nextcloud_db:
  nextcloud_data:
  portainer_data: